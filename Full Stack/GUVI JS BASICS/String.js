//STRING

/*let arr1=[1,2,3,4,5];
let string=arr1.join(',');   //String can also have index..and it gives each character according to index.
console.log(string[1]);

let str='javascript is a programming';
console.log(str[0]);
console.log(str.length);
console.log(str.split(''));  //convert String to Array....split can only able to use in string
console.log(str.split(' '));*/
/*let str='Hello!';
console.log(str.trimStart());
console.log(str.trimEnd());
console.log(str.substring(1,6));
console.log(str.toLowerCase());
console.log(str.toUpperCase());
console.log(str.slice(3));                    //   lo!
console.log(str.split('!').length>1);         //   true
console.log(str.charCodeAt(0));
console.log(str.codePointAt(65));*/
/*let words=['mango','watermelon','apple','jackfruit'];
function sorter(first,second){                  //-->Sort the string
    if(first<second)
      return -1;
    else if(first>second)
      return 1;
    else
       return 0;
}
words.sort(sorter);                     
console.log(words);*/


/*let first='apple';
let second='appim';
let twostring=[first,second];
function sorter(first,second){                  //-->Sort the string
    if(first<second)
      return -1;
    else if(first>second)
      return 1;
    else
       return 0;
}
twostring.sort(sorter);                     
console.log(twostring);*/
/*let arr=['guvigeek'];
let arr1=arr[0].split('');
console.log(arr1);
for(let i=0;i<arr1.length;i++)
{
  console.log(arr1[i]);
}*/

/*let arr=['guvi'];
 let str=arr[0];
 let res='';
 /*for(let i=0;i<str.length;i++)
 {
      if(i<str.length-1)
      {
        res=res+str[i]+',';
      }
      else
        res=res+str[i];
 }
 console.log(res);*/
 /*for(let i=0;i<str.length;i++)
 {   
        res=res+str[i]+',';
 }
 console.log(res.slice(0,res.length-1)); */     //Slice can used to fetch and take a particular elements


